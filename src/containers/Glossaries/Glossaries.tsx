import React from 'react'

export type GlossariesProps = {

}

const Glossaries: React.FC<GlossariesProps> = ({ children }) => {
  return (
    <div>
      Glossaries
    </div>
  )
}

export default React.memo(Glossaries)
